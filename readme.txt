To build it for testing purposes, 
1) Go to sites folder
2) npm run build

To push it to ngjhd.vercel.app
1) git add .
2) git commit -m ""
3) git push origin master

It will auto push to vercel, which will build and host it.